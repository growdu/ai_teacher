# Test configuration — uses real PostgreSQL/Redis/MinIO from Docker Compose
jwt:
  secret: test-jwt-secret-at-least-32-characters-long-for-hs256
  expiration: 86400000
  refresh-expiration: 604800000
  prefix: Bearer
  header: Authorization

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_teacher
    username: postgres
    password: ai_tea...2024
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
  main:
    allow-bean-definition-overriding: true

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: ai-teacher
  secure: false

# AI Provider test keys
ai:
  doubao:
    api-key: test
    endpoint: https://ark.cn-beijing.volces.com/api/v3
  deepseek:
    api-key: test
  openai:
    api-key: test
    base-url: https://api.openai.com

# Disable unnecessary auto-config for tests
payment:
  enabled: false
  sandbox-enabled: false
